# 브리핑 운영 배포·연결 검증

검증일: 2026-09-14 KST. 앱 코드 `2e1908a`와 기존 결함 수정 `78f9a4f`를 함께 배포했다.

## 적용 결과

- 운영: https://retirefarm.specialrisk.me/reports 의 주간 브리핑 탭.
- 이미지: `ghcr.io/hyunjoonkwak/retirefarm-manager:briefing-2e1908a` (linux/amd64).
- 배포 digest: `sha256:7c86258393c15c3d5911f456d27e320b1a29518d1850858ce0e27f0d43087433`.
- NAS `.env`의 IMAGE_TAG를 고정했고, 나머지 환경 설정을 유지했다. `deploy.sh`도 온라인 백업을 사용하는 버전으로 갱신했다.
- 새 migration `20260914030000_briefing_worker` 적용 및 세 테이블 생성 확인.
- readiness 세 항목 true, Docker healthy. 시세 원자료는 배포 전후 816,319건, 최신 경매일은 9월 14일 KST. 자동 원자료 삭제 OFF/보존 365일 설정 유지.
- 인증 없는 worker POST는 리다이렉트 없이 401. 브라우저에서 기존 월간 보고서와 새 주간 탭 확인.

## 백업·롤백

배포 직전 새 백업 스크립트를 기존 컨테이너의 별도 임시 파일로 실행했다. SQLite online backup 후 사본만 DELETE journal로 변환하고 quick_check·외래키·migration 검사를 통과했다. 운영 DB의 WAL 모드는 변경하지 않았다.

- 백업: `backup_2026-09-14T03-03-21-838Z_9a614470-1e19-43f0-914e-01ec6588feec.db`, 430,518,272 bytes, 운영 백업 볼륨.
- 이전 이미지: `sha256:5984fc82a9419ddfb754e27ecd901be66f6e7450c133b3b9c345ecce56e4d8d7`, NAS 로컬 태그 `retirefarm-manager:rollback-20260914`로 보존.
- NAS 배포 폴더에 `.env.before-briefing-20260914`와 `deploy.sh.before-briefing-20260914` 보존. 환경 사본은 0600, 비밀값은 문서·로그에 기록하지 않았다.
- 롤백 시 compose image를 위 로컬 롤백 태그로 명시하고 재생성한다. 새 테이블은 그대로 둘 수 있다. DB 전체 복원은 이후 수집 데이터를 잃을 수 있어 자동 수행하지 않는다.

## 실제 Mac 워커 검증

본인 로그인 계정 UI에서 전용 토큰을 발급하고 Mac 비공개 파일(0600)에 설치했다. Codex 로그인 파일이나 AI API 키를 NAS로 복사하지 않았다.

1. 작업 없는 상태에서 워커 실행: `no job available`, 종료 0, AI 생성 호출 없음.
2. UI에서 토마토 / 토마토(적색) / 전라북도 장수군으로 초안 요청.
3. 지난 완결 주(9월 7–13일) 중앙청과 자료의 다섯 비교 조건·열다섯 지표로 생성.
4. 작업 `cmu0nzgwe0006569vzj695fp2`는 한 번 실행해 SUCCEEDED, 보고서는 DRAFT로 저장. 약 37.8초, 입력 17,163 / 출력 894 / 캐시 0 토큰. 별도 API 청구 금액을 뜻하지 않는다.
5. 화면에서 네 영역·우선 행동 세 개·근거·집계 수치를 대조했다. 같은 단위 내 특 등급이 상 등급보다 비싸다는 설명이 3kg·5kg 집계와 일치했다. 전주 상승으로 오해하지 않도록 서술했고 미수집 세 영역은 추정하지 않았다.
6. 같은 입력을 다시 요청한 뒤 수동 실행기를 실행: 작업 한 건·시도 한 번 유지, `no job available`, 추가 AI 호출 없음.

수동 실행기: `~/.config/retirefarm/run-briefing-worker.command`. 현재 저장소 및 Node/Codex 절대 경로를 참조한다. 자동 예약·텔레그램은 미활성이다.

## 후속 개발 순서

1. **무인 실행 전 운영 보강**: 전달 불명확(종료 4) 결과의 보관 파일 재전송과 중복 추론 방지. 지금은 lease 만료 후 재실행 시 AI가 다시 호출될 수 있어 저장 상태를 먼저 확인한다. 동시에 같은 입력을 요청하는 경합을 기존 작업 반환으로 처리한다.
2. **원문 재현·데이터 신뢰도**: Work 전문/정정 이력 가져오기, 수집 누락 표시와 과거 복구, 공개 일평균 시계열과 원자료 가중평균을 구분한 전주·네 주 비교.
3. **가격 정리·검색 UX**: 산지별 관측 품종 선택, 동일 품종·산지·등급·단위 비교, 중앙값·분포·표본 수·이상치 표시. 20개 작업 전체 스냅샷을 목록에서 전송하는 구조는 상세 조회로 분리한다.
4. **외부 출처 연결**: 공식 네이버 쇼핑검색 권한 확인과 어댑터, 비교 가능한 고정 판매처 패널, 기상·재배·물류 자료 연결. 다른 종류의 네이버 키를 임의로 시험 전송하지 않는다.
5. **예약·발송**: Mac 가용성과 재시도 정책을 확정한 후 월요일 예약, 발송 이력·중복 방지와 텔레그램 dry-run. 실제 발송은 사용자 제공 봇/채팅방 정보 연결 후 진행한다.

Claude는 배포 차단 문제를 찾지 못했고 HTTPS/리다이렉트와 전달 불명확 상태의 재실행 주의점을 독립적으로 검토했다. 검토용 터미널은 완료 후 정리했다. 본 검증은 좁은 실자료 한 건이며 전체 Work 보고서 대체나 무인 운영 품질 보증은 아니다.
